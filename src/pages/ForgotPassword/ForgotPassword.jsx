// Recuperação de senha é backlog (PRD §4.1) — stub só pra "Esqueceu a senha?"
// (Login.jsx) ter uma rota de verdade em vez de link morto. Mesmo padrão de
// pages/Admin/Admin.jsx.
export default function ForgotPassword() {
  return (
    <div className="flex min-h-[50vh] flex-col items-center justify-center px-6 py-16 text-center">
      <h1 className="text-2xl font-semibold text-slate-800">Recuperação de senha</h1>
      <p className="mt-2 text-slate-500">Em construção.</p>
    </div>
  )
}
